# ═══════════════════════════════════════════════════════════════════════
# CONFIGURATION MINIMALE - OGAN FRAMEWORK
# ═══════════════════════════════════════════════════════════════════════
#
# Copiez ce fichier en .env : cp .env.example .env
# ⚠️ NE COMMITEZ JAMAIS le fichier .env dans Git !
#
# ═══════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────
# VARIABLES ESSENTIELLES (4 obligatoires en production)
# ─────────────────────────────────────────────────────────────────────

# Environnement : dev, prod, test
APP_ENV=dev

# Clé secrète (OBLIGATOIRE en prod)
# Générer avec : php -r "echo bin2hex(random_bytes(32));"
APP_SECRET=changeme-in-production

# Base de données (format Symfony)
DATABASE_URL="mysql://root:root@127.0.0.1:3306/ogan_framework?charset=utf8mb4"

# Mailer (optionnel - MailHog/localhost:1025 par défaut en dev)
# MAILER_DSN=smtp://user:pass@smtp.example.com:587

# ═══════════════════════════════════════════════════════════════════════
# OPTIONS AVANCÉES (auto-configurées selon APP_ENV)
# ═══════════════════════════════════════════════════════════════════════
# Décommentez SEULEMENT pour surcharger les défauts automatiques.
#
# ┌─────────────────────┬──────────────┬──────────────┬──────────────┐
# │ Variable            │ dev (défaut) │ prod         │ test         │
# ├─────────────────────┼──────────────┼──────────────┼──────────────┤
# │ APP_DEBUG           │ true         │ false        │ true         │
# │ SESSION_SECURE      │ false        │ true         │ false        │
# │ SESSION_LIFETIME    │ 7200 (2h)    │ 3600 (1h)    │ 7200         │
# │ SESSION_SAMESITE    │ Lax          │ Strict       │ Lax          │
# │ LOG_LEVEL           │ debug        │ error        │ warning      │
# └─────────────────────┴──────────────┴──────────────┴──────────────┘
#
# Exemples de surcharge :
# APP_DEBUG=false
# SESSION_LIFETIME=3600
# LOG_LEVEL=info
# ROUTER_BASE_PATH=/mon-sous-dossier
