{{ title = title ?? 'Mon App' }}
<!DOCTYPE html>
<html lang="fr" class="h-full">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ title }}</title>
		<link rel="stylesheet" href="{{ asset('/assets/css/app.css') }}">
	</head>

	{# ═══════════════════════════════════════════════════════════════════════════
			📌 HTMX SUPPORT
			HTMX est activé par défaut via app.js
			Pour utiliser le mode boost (SPA-like), ajoutez les attributs ci-dessous à <body>
			═══════════════════════════════════════════════════════════════════════════ #}

	{# VERSION AVEC HTMX BOOST (SPA-like navigation) :
			<body class="min-h-screen flex flex-col bg-gray-50" hx-boost="true" hx-target="#page-content" hx-swap="innerHTML" hx-select="#page-content">
				<div id="page-content"> #}

	<body class="min-h-screen flex flex-col bg-gray-50">

		{{ component('navbar') }}

		<main class="flex-1">
			{{ component('flashes') }}
			{{ section('body') }}
		</main>

		{{ component('footer') }}

		{# Décommenter pour HTMX BOOST : </div> #}

		{# Un seul script qui charge tout : HTMX + OganStimulus + Contrôleurs #}<script type="module" src="{{ asset('/assets/js/app.js') }}"> </script>
	</body>

</html>
