# Configuration Ogan Framework

app:
  env: '%env(APP_ENV)%'
  debug: true

view:
  templates_path: '%kernel.project_dir%/templates'
  default_layout: layouts/base.ogan
  default_title: Mon site
  use_compiler: true
  cache_dir: '%kernel.project_dir%/var/cache/templates'

router:
  base_path: ''

css_framework:
  provider: tailwind
  version: 4.1.18
  cdn: false
  custom_css: []

tailwind:
  input: assets/css/app.css
  output: public/assets/css/app.css
  minify: false

session:
  name: OGAN_SESSION
  lifetime: 7200
  path: /
  domain: ''
  secure: false
  httponly: true
  samesite: Lax

# ─────────────────────────────────────────────────────────────────────────────
# 📦 CACHE
# ─────────────────────────────────────────────────────────────────────────────
cache:
  driver: file
  path: '%kernel.project_dir%/var/cache/data'
  default_ttl: 3600

  stores:
    file:
      path: '%kernel.project_dir%/var/cache/data'
      ttl: 3600
    array:
      ttl: 3600

  queries:
    enabled: true
    ttl: 300

  routes:
    enabled: true
    file: '%kernel.project_dir%/var/cache/routes.php'

# ─────────────────────────────────────────────────────────────────────────────
# 🔐 AUTHENTIFICATION
# Configuration générée par: php bin/console make:auth
# ─────────────────────────────────────────────────────────────────────────────
auth:
  # Activer après avoir configuré l'email (voir section mail)
  send_verification_email: false
  send_password_reset_email: false

  # Redirections
  login_redirect: /              # Défaut pour les utilisateurs
  logout_redirect: /login

  # Redirection par rôle (optionnel)
  # Les rôles sont vérifiés dans l'ordre, le premier qui match est utilisé
  role_redirects:
    ROLE_ADMIN: /dashboard       # Les admins vont au dashboard
    # ROLE_AUTHOR: /my-articles  # Exemple: les auteurs vers leurs articles

# ─────────────────────────────────────────────────────────────────────────────
# 📧 EMAIL
# Configurer dans .env :
#   MAILER_DSN=smtp://user:pass@smtp.example.com:587
#   MAIL_FROM=noreply@example.com
#   MAIL_FROM_NAME="Mon App"
# ─────────────────────────────────────────────────────────────────────────────
mail:
  dsn: '%env(MAILER_DSN)%'
  from: '%env(MAIL_FROM)%'
  from_name: '%env(MAIL_FROM_NAME)%'

# ─────────────────────────────────────────────────────────────────────────────
# � UPLOADS & IMAGES
# ─────────────────────────────────────────────────────────────────────────────
uploads:
  directory: '%kernel.project_dir%/public/assets/uploads'  # Dossier par défaut
  quality: 85                                               # Qualité compression (1-100)
  format: webp                                              # Format de sortie par défaut

# ─────────────────────────────────────────────────────────────────────────────
# �🐛 DEBUG (désactiver en production)
# ─────────────────────────────────────────────────────────────────────────────
debug:
  enabled: true
  debug_bar: true
  collect_queries: true
  max_dump_depth: 10

# ─────────────────────────────────────────────────────────────────────────────
# 🛡️ SÉCURITÉ
# ─────────────────────────────────────────────────────────────────────────────
security:
  user_class: App\Model\User

  role_hierarchy:
    ROLE_ADMIN: [ROLE_USER]
    ROLE_SUPER_ADMIN: [ROLE_ADMIN]

  access_denied_url: /login

# ─────────────────────────────────────────────────────────────────────────────
# ⚡ HTMX (optionnel)
# Activer avec: php bin/console make:auth --htmx
# Ou décommenter manuellement ci-dessous
# ─────────────────────────────────────────────────────────────────────────────
frontend:
  htmx:
    enabled: false                       # Mettre true pour activer HTMX
    progress_bar: true                    # Barre de progression automatique
    script: /assets/js/htmx.min.js
